{
  "id": "Entity.fromObject",
  "displayName": "Entity.fromObject",
  "kind": "oop_function",
  "memberName": ".fromObject",
  "receiver": {
    "classId": "Entity",
    "kind": "instance",
    "typeRef": {
      "kind": "concrete",
      "name": "Entity"
    }
  },
  "summary": "Fills an entity with the *filler* content",
  "description": "The .fromObject() function fills an entity with the filler content.\n\nThis function modifies the original entity.\n\nThe mapping between the object and the entity is done on the attribute names:\n\nIf a property of the object does not exist in the dataclass, it is ignored. Data types must be equivalent. If there is a type mismatch between the object and dataclass, 4D tries to convert the data whenever possible (see Converting data types), otherwise the attribute is left untouched. The primary key can be given as is or with a \"__KEY\" property (filled with the primary key value). If it does not already exist in the dataclass, the entity is created with the given value when .save() is called. If the primary key is not given, the entity is created and the primary key value is assigned with respect to database rules. The auto-increment is only computed if the primary key is null.\n\nfiller can handle a related entity under the following conditions:\n\nfiller contains the foreign key itself, or filler contains a property object with the same name as the related entity, containing a single property named \"__KEY\". if the related entity does not exist, it is ignored.",
  "docPage": "mirror/docs/21-R3/API/EntityClass.html",
  "overloads": [
    {
      "params": [
        {
          "name": "filler",
          "type": {
            "kind": "concrete",
            "name": "Object"
          },
          "direction": "in",
          "optional": false,
          "description": "Object from which to fill the entity"
        }
      ],
      "rawSyntax": "**.fromObject**( *filler* : Object )",
      "returns": null
    }
  ],
  "sinceVersion": "17"
}
