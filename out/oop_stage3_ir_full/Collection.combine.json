{
  "id": "Collection.combine",
  "displayName": "Collection.combine",
  "kind": "oop_function",
  "memberName": ".combine",
  "receiver": {
    "classId": "Collection",
    "kind": "instance",
    "typeRef": {
      "kind": "concrete",
      "name": "Collection"
    }
  },
  "summary": "Inserts *col2* elements at the end or at the specified *index* position in the collection instance and returns the edited collection",
  "description": "The .combine() function inserts col2 elements at the end or at the specified index position in the collection instance and returns the edited collection. Unlike the .insert() function, .combine() adds each value of col2 in the original collection, and not as a single collection element.\n\nThis function modifies the original collection.\n\nBy default, col2 elements are added at the end of the orginal collection. You can pass in index the position where you want the col2 elements to be inserted in the collection.\n\nWarning: Keep in mind that collection elements are numbered from 0.\n\nIf index > the length of the collection, the actual starting index will be set to the length of the collection. If index < 0, it is recalculated as index:=index+length (it is considered as the offset from the end of the collection). If the calculated value is negative, index is set to 0.",
  "docPage": "mirror/docs/21-R3/API/CollectionClass.html",
  "overloads": [
    {
      "params": [
        {
          "name": "col2",
          "type": {
            "kind": "concrete",
            "name": "Collection"
          },
          "direction": "in",
          "optional": false,
          "description": "Collection to combine"
        },
        {
          "name": "index",
          "type": {
            "kind": "concrete",
            "name": "Integer"
          },
          "direction": "in",
          "optional": true,
          "description": "Position to which insert elements to combine in collection (default=length+1)"
        }
      ],
      "rawSyntax": "**.combine**( *col2* : Collection {; *index* : Integer } ) : Collection",
      "returns": {
        "kind": "concrete",
        "name": "Collection"
      }
    }
  ],
  "sinceVersion": "16-R6"
}
