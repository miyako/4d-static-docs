{
  "id": "STOP-SQL-SERVER",
  "displayName": "STOP SQL SERVER",
  "kind": "classic_command",
  "theme": "SQL",
  "overloads": [
    {
      "params": [],
      "semanticRole": "The STOP SQL SERVER command stops the integrated SQL server in the 4D application where it has been executed."
    }
  ],
  "constraints": [
    "Does nothing if the SQL server was not launched; does not affect the internal 4D SQL kernel, which remains available for internal queries."
  ]
}