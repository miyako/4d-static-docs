{
  "id": "Get-print-preview",
  "displayName": "Get print preview",
  "kind": "classic_command",
  "theme": "Printing",
  "overloads": [
    {
      "params": [],
      "semanticRole": "The Get print preview command returns True if the SET PRINT PREVIEW command was called with the True value in the current process."
    }
  ],
  "constraints": [
    "Reflects only the SET PRINT PREVIEW setting for the current process, which the user can still override in the print dialog before validating it; use Is in print preview to get the final, actual printing mode."
  ]
}