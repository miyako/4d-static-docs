{
  "id": "Get-Application-color-scheme",
  "displayName": "Get Application color scheme",
  "kind": "classic_command",
  "theme": "User Interface",
  "overloads": [
    {
      "params": [
        {
          "name": "*",
          "type": {
            "kind": "literal_symbols",
            "symbols": [
              "*"
            ]
          },
          "direction": "in",
          "optional": true
        }
      ],
      "returns": {
        "kind": "concrete",
        "name": "Text"
      },
      "semanticRole": "The Get Application color scheme command returns the name of the actual color scheme in use at the application level."
    }
  ],
  "constraints": [
    "On Windows, this command always returns \"light\", regardless of settings.",
    "The optional * parameter only matters when called from a component: it returns the color scheme of the host database instead of the component's own scheme."
  ]
}