{
  "id": "GET-MISSING-TABLE-NAMES",
  "displayName": "GET MISSING TABLE NAMES",
  "kind": "classic_command",
  "theme": "Structure Access",
  "overloads": [
    {
      "params": [
        {
          "name": "missingTables",
          "type": {
            "kind": "concrete",
            "name": "Text array"
          },
          "direction": "out",
          "optional": false,
          "description": "Names of tables whose data exists in the data file but are not defined in the current structure (e.g. after opening the data file with a newer structure that dropped them)."
        }
      ],
      "semanticRole": "Returns the names of tables present in the data file but missing from the current structure, so they can be reactivated with REGENERATE MISSING TABLE."
    }
  ]
}