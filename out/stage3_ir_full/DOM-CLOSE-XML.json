{
  "id": "DOM-CLOSE-XML",
  "displayName": "DOM CLOSE XML",
  "kind": "classic_command",
  "theme": "XML-DOM",
  "overloads": [
    {
      "params": [
        {
          "name": "elementRef",
          "type": {
            "kind": "concrete",
            "name": "Text"
          },
          "direction": "in",
          "optional": false
        }
      ],
      "semanticRole": "Frees the memory occupied by an XML tree, ending its DOM lifecycle."
    }
  ],
  "protocolRefs": {
    "multiCallChain": "DOM XML document lifecycle: create/parse ref -> navigate/get/set elements and attributes -> export/close"
  },
  "constraints": [
    "elementRef must be an XML root object (e.g. as returned by DOM Create XML Ref or DOM Parse XML source/variable); otherwise an error is generated."
  ]
}