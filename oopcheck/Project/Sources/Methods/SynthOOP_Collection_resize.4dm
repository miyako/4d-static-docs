// overload 0
var $receiver : Collection
$receiver:=New collection(1; 2; 3)
var $result1 : Collection
$result1:=$receiver.resize(1; "synthText")
// overload 0 [optional:omit-defaultValue]
$receiver:=New collection(1; 2; 3)
var $result2 : Collection
$result2:=$receiver.resize(1)
