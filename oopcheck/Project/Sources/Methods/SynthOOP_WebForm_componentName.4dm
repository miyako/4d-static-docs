// property read
// dynamic member: the doc's `.componentName` is a name pattern, so a concrete member of the check project's own model stands in for it.
var $read1 : 4D.WebFormItem
$read1:=Web Form.myButton
