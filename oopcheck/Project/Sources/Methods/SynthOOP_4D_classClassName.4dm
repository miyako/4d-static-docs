// property read
// dynamic member: the doc's `.classClassName` is a name pattern, so a concrete member of the check project's own model stands in for it.
var $read1 : 4D.Class
$read1:=cs.SynthOOPHandler
