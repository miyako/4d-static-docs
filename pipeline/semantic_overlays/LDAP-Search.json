{
  "constraints": [
    "Must be executed within a connection to an LDAP server already opened with LDAP LOGIN; otherwise error 1003 is returned.",
    "scope must be one of the 'LDAP' theme constants (\"base\" default, \"one\", or \"sub\")."
  ]
}
