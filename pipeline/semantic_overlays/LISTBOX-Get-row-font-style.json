{
  "overloadMerges": [
    {
      "sourceIndices": [
        0,
        1
      ],
      "note": "The docs render two syntax lines that differ only in whether the leading '*' is passed (selecting whether 'object' is an object-name string vs. a form variable reference). That's one semantic overload with an optional literal-symbol discriminator, not two independent overloads.",
      "params": [
        {
          "name": "*",
          "type": {
            "kind": "literal_symbols",
            "symbols": [
              "*"
            ]
          },
          "direction": "in",
          "optional": true,
          "description": "If present, object is a form object name (string); if omitted, object is a variable reference."
        },
        {
          "name": "object",
          "type": [
            {
              "kind": "concrete",
              "name": "Text"
            },
            {
              "kind": "concrete",
              "name": "Variable"
            }
          ],
          "direction": "in",
          "optional": false,
          "description": "Form object name (if * is passed) or variable reference (if * is omitted), designating the target list box, column, header, or footer."
        },
        {
          "name": "row",
          "type": {
            "kind": "concrete",
            "name": "Integer"
          },
          "direction": "in",
          "optional": false,
          "description": "Row (or, when object is a column, cell) number whose font style is requested."
        }
      ],
      "returns": {
        "kind": "concrete",
        "name": "Integer"
      },
      "semanticRole": "Returns the font style of a row or cell in an array-type list box."
    }
  ],
  "constraints": [
    "Only works with array-type list boxes.",
    "A style assigned to a row is not necessarily what's displayed in every cell (column/cell-level styles can take priority)."
  ]
}
