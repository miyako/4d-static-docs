{
  "constraints": [
    "The command does not list objects of inherited forms or of subforms.",
    "If the command detects a duplicated method name, error -9802 (\"Object path not unique\") is generated."
  ],
  "overloadMerges": [
    {
      "sourceIndices": [0],
      "note": "arrPaths is filled in by the command (parameter table marks it with the '\u2190' return arrow), so its direction should be 'out'. stamp is bidirectional (parameter table marks it with the '\u2194' arrow: caller passes a minimum stamp value in, command returns the new current stamp value out), so its direction should be 'inout', not the syntax-position default of 'in'.",
      "params": [
        {
          "name": "aTable",
          "type": { "kind": "concrete", "name": "Table" },
          "direction": "in",
          "optional": true,
          "description": "Table whose table-form object methods to return; if omitted, returns objects of the database project forms instead."
        },
        {
          "name": "arrPaths",
          "type": { "kind": "concrete", "name": "Text array" },
          "direction": "out",
          "optional": false,
          "description": "Array of method paths and names"
        },
        {
          "name": "filter",
          "type": { "kind": "concrete", "name": "Text" },
          "direction": "in",
          "optional": true,
          "description": "Name filter; supports '@' for \"starts with\"/\"ends with\"/\"contains\"; an empty string is ignored."
        },
        {
          "name": "stamp",
          "type": { "kind": "concrete", "name": "Real" },
          "direction": "inout",
          "optional": true,
          "description": "In: minimum modification-counter stamp value to filter on. Out: the new current (highest) stamp value, for use in a subsequent call to get only newer/modified methods."
        },
        {
          "name": "*",
          "type": { "kind": "literal_symbols", "symbols": ["*"] },
          "direction": "in",
          "optional": true,
          "description": "If passed, applies to the host database's methods when called from a component (otherwise ignored)."
        }
      ]
    }
  ]
}
