{
  "theme": "List-Box",
  "overloadMerges": [
    {
      "sourceIndices": [0, 1],
      "note": "The docs render two syntax lines that differ only in whether the leading '*' is passed (selecting whether 'object' is an object-name string vs. a form variable reference). That's one semantic overload with an optional literal-symbol discriminator, not two independent overloads.",
      "params": [
        {
          "name": "*",
          "type": {"kind": "literal_symbols", "symbols": ["*"]},
          "direction": "in",
          "optional": true,
          "description": "If present, object is a form object name (string); if omitted, object is a variable reference."
        },
        {
          "name": "object",
          "type": [
            {"kind": "concrete", "name": "Text"},
            {"kind": "concrete", "name": "Variable"}
          ],
          "direction": "in",
          "optional": false,
          "description": "Form object name (if * is passed) or variable reference (if * is omitted), designating the target list box or header."
        },
        {
          "name": "height",
          "type": {"kind": "concrete", "name": "Integer"},
          "direction": "in",
          "optional": false,
          "description": "New header row height, in the unit given by unit."
        },
        {
          "name": "unit",
          "type": {"kind": "concrete", "name": "Integer"},
          "direction": "in",
          "optional": true,
          "description": "Unit of the height value.",
          "sentinelValues": [
            {"value": 0, "meaning": "Height expressed as a number of pixels (default if omitted)."},
            {"value": 1, "meaning": "Height expressed as a number of lines; 4D calculates line height from the font."}
          ]
        }
      ],
      "semanticRole": "Sets the height of the header row of the designated list box (or of a specific header)."
    }
  ],
  "constraints": [
    "Headers must respect the platform minimum height (24 px on Windows, 17 px on macOS); a lower value is clamped up to that minimum."
  ]
}
