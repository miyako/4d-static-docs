{
  "overloadPatches": [
    {
      "index": 0,
      "semanticRole": "Returns, in alphabetical order, the list of distinct object attribute paths found in an indexed Object field for the current selection."
    }
  ],
  "overloadMerges": [
    {
      "sourceIndices": [0],
      "note": "pathArray is a filled/returned array (docs: 'Array to receive list of distinct paths'); autoparse defaults all params to direction:in.",
      "params": [
        {
          "name": "objectField",
          "type": { "kind": "concrete", "name": "Field" },
          "direction": "in",
          "optional": false,
          "description": "Indexed object field; must be an indexed Object-type field or an error is returned"
        },
        {
          "name": "pathArray",
          "type": { "kind": "concrete", "name": "Text array" },
          "direction": "out",
          "optional": false,
          "description": "Receives the distinct attribute paths (dot notation), in alphabetical (diacritic) order"
        }
      ]
    }
  ],
  "constraints": [
    "objectField must be an indexed Object-type field; otherwise an error is returned.",
    "Records with an undefined value in objectField are not taken into account.",
    "Does not change the current selection or the current record."
  ]
}
