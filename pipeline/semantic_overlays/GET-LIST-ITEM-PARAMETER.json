{
  "constraints": [
    "selector should be one of the 'Additional text' or 'Associated standard action' constants from the Hierarchical Lists theme, or a custom string; see SET LIST ITEM PARAMETER for the selector/value contract."
  ],
  "overloadMerges": [
    {
      "sourceIndices": [0, 1],
      "note": "The two doc syntax lines differ only in whether a leading '*' is passed (list as object-name string vs. list reference number). Merged into one semantic overload with that param optional. Also corrected direction: value is filled in by the command ('\u2190' in the docs), not passed in; added the 0/'*' sentinel values documented for itemRef.",
      "params": [
        {
          "name": "*",
          "type": { "kind": "literal_symbols", "symbols": ["*"] },
          "direction": "in",
          "optional": true,
          "description": "If present, 'list' below is an object name (string); if omitted, 'list' is a hierarchical list reference number."
        },
        {
          "name": "list",
          "type": [
            { "kind": "concrete", "name": "Text" },
            { "kind": "concrete", "name": "Integer" }
          ],
          "direction": "in",
          "optional": false
        },
        {
          "name": "itemRef",
          "type": [
            { "kind": "concrete", "name": "Integer" },
            { "kind": "pseudo", "name": "Operator" }
          ],
          "direction": "in",
          "optional": false,
          "sentinelValues": [
            { "value": 0, "meaning": "the last item added to the list (via APPEND TO LIST)" },
            { "value": "*", "meaning": "the current item of the list" }
          ]
        },
        {
          "name": "selector",
          "type": { "kind": "concrete", "name": "Text" },
          "direction": "in",
          "optional": false
        },
        {
          "name": "value",
          "type": { "kind": "concrete", "name": "Variable" },
          "direction": "out",
          "optional": false,
          "description": "Current value of the parameter designated by selector (a variable of type Text, Boolean, or Real)."
        }
      ]
    }
  ]
}
