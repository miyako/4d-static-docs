{
  "constraints": [
    "Sizes are constant across pages within the same print job (they do not change after a page break)."
  ],
  "overloadMerges": [
    {
      "sourceIndices": [0],
      "note": "Corrected direction: height and width are filled in by the command ('\u2190' in the docs), not passed in.",
      "params": [
        {
          "name": "height",
          "type": { "kind": "concrete", "name": "Integer" },
          "direction": "out",
          "optional": false,
          "description": "Height of the printable area, in pixels."
        },
        {
          "name": "width",
          "type": { "kind": "concrete", "name": "Integer" },
          "direction": "out",
          "optional": true,
          "description": "Width of the printable area, in pixels."
        }
      ]
    }
  ]
}
