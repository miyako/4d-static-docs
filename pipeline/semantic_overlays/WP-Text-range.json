{
  "theme": "WritePro",
  "overloadMerges": [
    {
      "sourceIndices": [0],
      "note": "startRange/endRange are typed Integer for arbitrary character offsets, but the doc prose also documents two reserved constants (wk start text, wk end text) that can be passed to mean 'beginning of document' / 'end of document'; restored as explicit sentinelValues alongside the plain-Integer usage.",
      "params": [
        {
          "name": "targetObj",
          "type": {"kind": "concrete", "name": "Object"},
          "direction": "in",
          "optional": false,
          "description": "Range or element or 4D Write Pro document"
        },
        {
          "name": "startRange",
          "type": {"kind": "concrete", "name": "Integer"},
          "direction": "in",
          "optional": false,
          "description": "Starting offset of range in the area",
          "sentinelValues": [
            {"value": "wk start text", "meaning": "Beginning of the document/element (or of the element range if targetObj is an element reference)"}
          ]
        },
        {
          "name": "endRange",
          "type": {"kind": "concrete", "name": "Integer"},
          "direction": "in",
          "optional": false,
          "description": "Ending offset of range in the area",
          "sentinelValues": [
            {"value": "wk end text", "meaning": "End of the document/element (or of the element range if targetObj is an element reference)"}
          ]
        }
      ],
      "returns": {"kind": "concrete", "name": "Object"},
      "semanticRole": "Returns a new range object addressing the character selection between startRange and endRange within targetObj, usable for reading/writing attributes via WP GET ATTRIBUTES / WP SET ATTRIBUTES."
    }
  ],
  "constraints": [
    "If targetObj is not a valid object, an empty range is returned.",
    "A 4D Write Pro document contains not only visible text but also formatting tags, which are included in the range.",
    "If targetObj is an element (table/row/paragraph/inline picture/body/header/footer/section/subsection), startRange and endRange are relative to that element's own start/end, not the whole document; for a section specifically, they are relative to the document body (the section's parent range)."
  ]
}
