{
  "constraints": [
    "The custom parameter must have been set beforehand using the SET MENU ITEM PARAMETER command."
  ],
  "overloadMerges": [
    {
      "sourceIndices": [0],
      "note": "Added the -1 sentinel value documented for menuItem. Restored `returns` (Text) from the parameter table's 'Function result' row -- apply_overload_merges replaces the whole overload from this merge entry's fields, so it was silently dropped even though the mechanical draft had correctly computed it.",
      "semanticRole": "Get the custom character-string parameter previously attached to a menu item via SET MENU ITEM PARAMETER.",
      "params": [
        {
          "name": "menu",
          "type": [
            { "kind": "concrete", "name": "Integer" },
            { "kind": "concrete", "name": "Text" }
          ],
          "direction": "in",
          "optional": false,
          "description": "Menu reference (MenuRef) or menu number."
        },
        {
          "name": "menuItem",
          "type": { "kind": "concrete", "name": "Integer" },
          "direction": "in",
          "optional": false,
          "sentinelValues": [
            { "value": -1, "meaning": "the last item added to menu" }
          ]
        }
      ],
      "returns": {"kind": "concrete", "name": "Text"}
    }
  ]
}
