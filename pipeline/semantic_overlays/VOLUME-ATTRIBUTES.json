{
  "theme": "System-Documents",
  "overloadMerges": [
    {
      "sourceIndices": [0],
      "note": "The mechanical parser defaulted size/used/free to direction 'in', but the parameter table arrow ('\u2190') and description ('returns ... the size, the used space and the free space') show they are output parameters filled in by the command.",
      "params": [
        {
          "name": "volume",
          "type": { "kind": "concrete", "name": "Text" },
          "direction": "in",
          "optional": false,
          "description": "Volume name"
        },
        {
          "name": "size",
          "type": { "kind": "concrete", "name": "Real" },
          "direction": "out",
          "optional": false,
          "description": "Volume size expressed in bytes"
        },
        {
          "name": "used",
          "type": { "kind": "concrete", "name": "Real" },
          "direction": "out",
          "optional": false,
          "description": "Used space expressed in bytes"
        },
        {
          "name": "free",
          "type": { "kind": "concrete", "name": "Real" },
          "direction": "out",
          "optional": false,
          "description": "Free space expressed in bytes"
        }
      ]
    }
  ],
  "constraints": [
    "If volume designates a non-mounted remote volume, the system variable OK is set to 0 and size/used/free are each returned as -1."
  ]
}
