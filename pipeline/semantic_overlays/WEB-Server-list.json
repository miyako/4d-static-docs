{
  "theme": "Web-Server",
  "overloadMerges": [
    {
      "sourceIndices": [
        0
      ],
      "note": "Autoparse could not find a top-level '(...)' parameter list in the no-parentheses signature 'WEB Server list : Collection', so it silently dropped the return type. Restored from the parameterTable's Function result row.",
      "params": [],
      "returns": {
        "kind": "concrete",
        "name": "Collection"
      },
      "semanticRole": "Returns a collection of all available 4D.WebServer objects (the database's own Web server plus any host/component Web servers)."
    }
  ]
}