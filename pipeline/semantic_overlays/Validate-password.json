{
  "constraints": [
    "Execution is throttled to prevent brute-force attacks: after the 4th call, subsequent calls are delayed by 10 seconds; this delay applies workstation-wide, not per-process."
  ]
}
