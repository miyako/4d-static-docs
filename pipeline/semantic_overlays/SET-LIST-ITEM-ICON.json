{
  "theme": "Hierarchical-Lists",
  "overloadMerges": [
    {
      "sourceIndices": [0, 1],
      "note": "The docs render two syntax lines that differ only in whether a leading '*' is passed (selecting whether 'list' is a name-string vs. a reference-number, and correspondingly Text vs. Integer). That's one semantic overload with an optional literal-symbol discriminator, not two overloads. Also restores itemRef's Operator '*' sentinel meaning.",
      "params": [
        {
          "name": "asObjectName",
          "type": { "kind": "literal_symbols", "symbols": ["*"] },
          "direction": "in",
          "optional": true,
          "description": "If present, 'list' below is an object name (string) referring to a form representation of the list; if omitted, 'list' is a hierarchical list reference number (ListRef)."
        },
        {
          "name": "list",
          "type": [
            { "kind": "concrete", "name": "Integer" },
            { "kind": "concrete", "name": "Text" }
          ],
          "direction": "in",
          "optional": false,
          "description": "List reference number (if '*' omitted), or name of the list-type form object (if '*' passed)."
        },
        {
          "name": "itemRef",
          "type": [
            { "kind": "concrete", "name": "Integer" },
            { "kind": "pseudo", "name": "Operator" }
          ],
          "direction": "in",
          "optional": false,
          "sentinelValues": [
            { "value": 0, "meaning": "the last item appended to the list (via APPEND TO LIST)" },
            { "value": "*", "meaning": "the current item of the list (requires the object-name syntax, since each form representation can have its own current item)" }
          ]
        },
        {
          "name": "icon",
          "type": [
            { "kind": "concrete", "name": "Picture" },
            { "kind": "concrete", "name": "Pointer" }
          ],
          "direction": "in",
          "optional": false
        }
      ],
      "interactionNotes": [
        {
          "params": ["asObjectName (leading *)", "itemRef (* sentinel)"],
          "note": "The literal symbol '*' means two unrelated things depending on position: leading = 'list is a name, not a ref number'; in itemRef's slot = 'operate on the current item'. Using the itemRef '*' sentinel requires the object-name syntax (leading '*' passed)."
        }
      ]
    }
  ],
  "constraints": [
    "Unlike SET LIST ITEM PROPERTIES (which only accepts static picture references), this command's icon parameter also accepts a Pointer, so dynamic pictures can be used."
  ]
}
