{
  "theme": "XML-DOM",
  "constraints": [
    "Searches are case sensitive.",
    "When arrElementRefs is passed and filled, the command's function-result return value is the first element of that array."
  ],
  "overloadMerges": [
    {
      "sourceIndices": [0],
      "note": "arrElementRefs is a filled/returned array (docs: 'List of element references found'); autoparse defaults all params to direction:in.",
      "params": [
        {
          "name": "elementRef",
          "type": { "kind": "concrete", "name": "Text" },
          "direction": "in",
          "optional": false,
          "description": "XML element reference at which the search starts"
        },
        {
          "name": "xPath",
          "type": { "kind": "concrete", "name": "Text" },
          "direction": "in",
          "optional": false,
          "description": "XPath expression identifying the element(s) to look for"
        },
        {
          "name": "arrElementRefs",
          "type": { "kind": "concrete", "name": "Text array" },
          "direction": "out",
          "optional": true,
          "description": "Receives the list of all matching element references, useful when several elements match xPath"
        }
      ]
    }
  ],
  "overloadPatches": [
    {
      "index": 0,
      "semanticRole": "Searches an XML structure, starting at elementRef, for element(s) matching an XPath expression, returning the (first) match's reference and optionally all matches."
    }
  ]
}
