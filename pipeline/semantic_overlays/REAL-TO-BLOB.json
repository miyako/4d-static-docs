{
  "overloadMerges": [
    {
      "sourceIndices": [0, 1],
      "note": "The two doc syntax lines only differ in which single trailing optional param is present (offset vs '*'); they are mutually exclusive alternate tails of one overload, not independent overloads. Also fixed offset's direction to inout per its arrow-marked (\u2194) 'new offset after writing' behavior, which the draft parser had defaulted to in.",
      "params": [
        {
          "name": "real",
          "type": { "kind": "concrete", "name": "Real" },
          "direction": "in",
          "optional": false,
          "description": "Real value to write into the BLOB."
        },
        {
          "name": "blob",
          "type": { "kind": "concrete", "name": "Blob" },
          "direction": "in",
          "optional": false,
          "description": "BLOB to receive the Real value; its size is adjusted to fit the written value."
        },
        {
          "name": "realFormat",
          "type": { "kind": "concrete", "name": "Integer" },
          "direction": "in",
          "optional": false,
          "description": "Internal format/byte-ordering constant for the written value: 0 Native, 1 Extended, 2 Macintosh Double, 3 Windows/PC Double.",
          "sentinelValues": [
            { "value": 0, "meaning": "Native real format" },
            { "value": 1, "meaning": "Extended real format" },
            { "value": 2, "meaning": "Macintosh Double real format" },
            { "value": 3, "meaning": "Windows/PC Double real format" }
          ]
        },
        {
          "name": "offset",
          "type": { "kind": "concrete", "name": "Variable" },
          "direction": "inout",
          "optional": true,
          "description": "Byte offset (from zero) within blob at which to write; on return, incremented by the number of bytes written so it can be reused for a subsequent BLOB write."
        },
        {
          "name": "*",
          "type": { "kind": "literal_symbols", "symbols": ["*"] },
          "direction": "in",
          "optional": true,
          "description": "If passed, append the Real value at the end of blob instead of writing at the start or at offset."
        }
      ],
      "semanticRole": "Writes real into blob in the byte format/ordering selected by realFormat, either at the start of the BLOB, at a caller-supplied offset, or appended at the end.",
      "jointConstraints": [
        {
          "params": ["offset", "*"],
          "rule": "offset and '*' are mutually exclusive alternate tails of the call; at most one of them may be passed. If neither is passed, the value is written at the start of blob, overwriting its previous contents."
        }
      ]
    }
  ],
  "constraints": [
    "New bytes allocated by extending the BLOB (other than the value being written) are zero-initialized.",
    "Does not support blob objects (4D.Blob type); it mutates the classic BLOB parameter in place, so blob must be a legacy BLOB, not a 4D.Blob."
  ]
}
