{
  "theme": "Objects-Forms",
  "overloadMerges": [
    {
      "sourceIndices": [
        0,
        1
      ],
      "note": "The two doc syntax lines differ only in whether the leading '*' is passed (selecting whether object is a name-string vs. a Variable/Field reference); collapsed into one overload with an optional literal-symbol discriminator.",
      "params": [
        {
          "name": "asObjectName",
          "type": {
            "kind": "literal_symbols",
            "symbols": [
              "*"
            ]
          },
          "direction": "in",
          "optional": true,
          "description": "If specified, object is an object name (string); if omitted, object is a variable or a field."
        },
        {
          "name": "object",
          "type": [
            {
              "kind": "concrete",
              "name": "Text"
            },
            {
              "kind": "concrete",
              "name": "Variable"
            },
            {
              "kind": "concrete",
              "name": "Field"
            }
          ],
          "direction": "in",
          "optional": false,
          "description": "Object name (if * is specified) or Variable or field (if * is omitted)"
        },
        {
          "name": "listType",
          "type": {
            "kind": "concrete",
            "name": "Integer"
          },
          "direction": "in",
          "optional": true,
          "description": "Type of list to set: Choice list, Required list, or Excluded list; defaults to Choice list (0) if omitted.",
          "sentinelValues": [
            {
              "value": 0,
              "meaning": "Choice list -- simple list of values to choose from (default if listType is omitted)"
            },
            {
              "value": 1,
              "meaning": "Required list -- lists only values accepted for entry"
            },
            {
              "value": 2,
              "meaning": "Excluded list -- lists values not accepted for entry"
            }
          ],
          "default": 0
        },
        {
          "name": "list",
          "type": {
            "kind": "concrete",
            "name": "Text"
          },
          "direction": "in",
          "optional": false,
          "description": "Name of the list to use as the given listType, or \"\" to disassociate the list of that type"
        }
      ],
      "semanticRole": "Associates, replaces, or (if list is empty) removes the named-list association of the given type (choice/required/excluded) for the object(s) designated by object and *."
    }
  ],
  "constraints": [
    "Can only be used in input/dialog forms, on fields and enterable variables whose value can be entered as text.",
    "list must already exist as a list created via the List Editor in Design mode.",
    "Cannot be used with fields located in a subform's list form.",
    "Existing values are replaced differently depending on how the list is bound to the object: a plain choice-list association replaces the list itself; an array-bound list is replaced by copying into array elements; an object-bound list (not supported in binary databases) is copied as collection elements into the object's values property."
  ]
}
