{
  "overloadMerges": [
    {
      "sourceIndices": [0],
      "note": "Added sentinelValues for descFormat's documented Queries-theme constants; the draft parser only captured the generic Integer type. Restored `returns` (Text) from the parameter table's 'Function result' row -- apply_overload_merges replaces the whole overload from this merge entry's fields, so it was silently dropped even though the mechanical draft had correctly computed it.",
      "params": [
        {
          "name": "descFormat",
          "type": {"kind": "concrete", "name": "Integer"},
          "direction": "in",
          "optional": false,
          "description": "Output format for the returned description.",
          "sentinelValues": [
            {"value": 0, "meaning": "Description in text format."},
            {"value": 1, "meaning": "Description in XML format."}
          ]
        }
      ],
      "returns": {"kind": "concrete", "name": "Text"}
    }
  ],
  "constraints": [
    "Returns a significant value only if DESCRIBE QUERY EXECUTION has been executed during the current session."
  ]
}
