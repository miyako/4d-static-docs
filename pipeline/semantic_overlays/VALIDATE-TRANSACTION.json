{
  "constraints": [
    "Transactions can be nested (sub-transactions); if the main (outer) transaction is later cancelled, all sub-transactions are cancelled even if individually validated with this command.",
    "Validates the transaction started by START TRANSACTION at the corresponding nesting level in the current process."
  ]
}
