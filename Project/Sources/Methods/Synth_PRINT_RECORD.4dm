// overload 0
PRINT RECORD([SynthTable];*)
// overload 0 flag-sweep omit-trailing-from:*
PRINT RECORD([SynthTable])
// overload 1
PRINT RECORD([SynthTable];>)
// overload 1 flag-sweep omit-trailing-from:>
PRINT RECORD([SynthTable])
